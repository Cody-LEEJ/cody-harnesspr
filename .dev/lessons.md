# Lessons

phase마다 `/retro`가 반복된 실패 패턴을 여기에 누적한다. 같은 패턴이 3개 phase에서 나타나면 `docs/GOLDEN_RULES.md`로 승격한다.

형식:

```
## {YYYY-MM-DD} {task}
- [패턴] {무엇이 반복됐는가 — step 번호, 횟수}
  → [대응] {다음에 step 설계/프롬프트/AC를 어떻게 바꾸면 막히는가}
```

---
