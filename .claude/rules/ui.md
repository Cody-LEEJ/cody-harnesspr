---
paths:
  - "src/components/**"
  - "src/app/**/*.tsx"
---

UI 파일을 만지기 전에 `docs/UI_GUIDE.md`를 읽어라. 이 규칙은 위 경로의 파일을 읽거나 수정할 때만 로드된다 — `execute.py` 경로에서는 step의 `docs` 선언이 같은 역할을 한다.

`docs/UI_GUIDE.md` 최상단에 `⚠ TEMPLATE` 배너가 남아 있으면 그 내용은 규칙이 아니다. 사용자에게 먼저 채우게 하라.
